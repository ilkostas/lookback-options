# Reference scripts and material

This folder contains the **original script implementations** and the reference document (`lookback.txt`) for European lookback options. These are for reference only and are not part of the installable `lookback_options` library (which lives in `src/lookback_options` at the project root).
